import { createTheme } from "@mui/material";

export const theme = createTheme({
	palette: {
		primary: {
			contrastText: "#000000",
			main: "#ffa6009e",
			dark: "#ffa#835705e4",
		},
		warning: {
			main: "#ffa6008e",
			dark: "rgba(178, 116, 0, 1)",
		},
	},
});
